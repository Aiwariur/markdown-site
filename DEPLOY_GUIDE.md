# 🚀 Деплой на Netlify - Пошаговая инструкция

## Шаг 1: Создание GitHub репозитория

1. Перейди на [GitHub](https://github.com) и войди в аккаунт
2. Нажми "New repository" (зеленая кнопка)
3. Назови репозиторий, например: `my-markdown-blog`
4. Сделай репозиторий **Public** (для бесплатного Netlify)
5. НЕ добавляй README, .gitignore или лицензию (у нас уже есть код)
6. Нажми "Create repository"

## Шаг 2: Подключение локального репозитория

Скопируй команды из GitHub (они будут показаны после создания репозитория):

```bash
git remote add origin https://github.com/ВАШ_USERNAME/my-markdown-blog.git
git branch -M main
git push -u origin main
```

Выполни эти команды в папке markdown-site.

## Шаг 3: Настройка Netlify

1. Перейди на [Netlify](https://www.netlify.com)
2. Зарегистрируйся или войди в аккаунт
3. Нажми "Add new site" → "Import an existing project"
4. Выбери "Deploy with GitHub"
5. Авторизуй Netlify для доступа к GitHub
6. Выбери свой репозиторий `my-markdown-blog`

## Шаг 4: Настройка сборки

В настройках деплоя укажи:

**Build command:**
```
npm ci --include=dev && npx convex deploy --cmd 'npm run build'
```

**Publish directory:**
```
dist
```

**Environment variables** (очень важно!):
- `CONVEX_DEPLOY_KEY` - получи из [Convex Dashboard](https://dashboard.convex.dev) → Project Settings → Deploy Key
- `VITE_CONVEX_URL` - `https://grateful-oriole-573.convex.cloud`

## Шаг 5: Деплой

1. Нажми "Deploy site"
2. Дождись завершения сборки (3-5 минут)
3. Получи URL своего сайта (например: `https://amazing-site-123.netlify.app`)

## Шаг 6: Настройка домена (опционально)

1. В настройках Netlify → Domain management
2. Можешь изменить поддомен или подключить свой домен

## 🎉 Готово!

Твой блог теперь доступен в интернете! 

**Как обновлять контент:**
1. Редактируй файлы в `content/blog/`
2. Запускай `npm run sync:prod` для обновления контента
3. Или делай `git push` для обновления кода

**Полезные ссылки:**
- Твой сайт: [будет показан после деплоя]
- Convex Dashboard: https://dashboard.convex.dev
- Netlify Dashboard: https://app.netlify.com

## Troubleshooting

**Если сборка падает:**
- Проверь переменные окружения
- Убедись что CONVEX_DEPLOY_KEY правильный
- Проверь логи сборки в Netlify

**Если контент не обновляется:**
- Запусти `npm run sync:prod` локально
- Проверь что Convex продакшн деплой работает